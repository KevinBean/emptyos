[app]
id = "release"
name = "Release"
version = "1.0.0"
description = "Package EmptyOS for distribution — safety checks, tier packaging"
dimensions = ["occupational"]

[app.entry]
module = "app"
class = "ReleaseApp"

[requires]
capabilities = []
apps = ["app-analytics", "tests"]

[provides.cli]
commands = []

[provides.web]
prefix = "/release"

[provides.events]
emits = ["release:packaged"]
